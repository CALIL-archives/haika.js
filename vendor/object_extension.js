// Generated by CoffeeScript 1.3.1

$.extend(fabric.Object.prototype, {
  currentZoom: 1,
  zoomable: true,
  setZoom: function(zoom) {
    var left, scaleX, scaleY, tempLeft, tempScaleX, tempScaleY, tempTop, top;
    if (this.zoomable) {
      scaleX = this.scaleX / this.currentZoom;
      scaleY = this.scaleY / this.currentZoom;
      left = this.left / this.currentZoom;
      top = this.top / this.currentZoom;
      tempScaleX = scaleX * zoom;
      tempScaleY = scaleY * zoom;
      tempLeft = left * zoom;
      tempTop = top * zoom;
      this.scaleX = tempScaleX;
      this.scaleY = tempScaleY;
      this.left = tempLeft;
      this.top = tempTop;
      this.currentZoom = zoom;
      return this.setCoords();
    }
  }
});
